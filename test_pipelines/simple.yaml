name: simple-test
version: 1.0.0
jobs:
  build:
    runtime: alpine:3.18
    resources:
      cpu: 0.5
      memory: 256Mi
    steps:
      - name: hello
        run: "echo 'Hello from Forge!'"
      - name: make-file
        run: "echo 'built' > out.txt && cat out.txt"
