name: cycle-test
version: 1.0.0
jobs:
  job-a:
    runtime: alpine:3.18
    needs: [job-b]
    steps:
      - name: step
        run: "echo 'a'"
  job-b:
    runtime: alpine:3.18
    needs: [job-a]
    steps:
      - name: step
        run: "echo 'b'"
