name: escape-test
version: 1.0.0
jobs:
  build:
    runtime: alpine:3.18
    resources:
      cpu: 0.5
      memory: 256Mi
    steps:
      - name: try-escape
        run: "cat /etc/hostname && ls / && cat /proc/1/environ || echo 'contained'"
